# Логика сервиса сравнения — приточные и приточно-вытяжные установки

Документ описывает архитектуру данных, разделение на два типа установок, новую
методику присвоения баллов (0–100) и план изменений по файлам. Составлен до начала
реализации (по запросу: «продумай и пропиши логику и промты»).

---

## 1. Цель доработки

1. Логотип **SHUFT** в левом верхнем углу.
2. Два вида установок: **приточные** и **приточно-вытяжные (ПВУ)**, с чётким
   разграничением в интерфейсе — сравнивать приточные отдельно от ПВУ и наоборот.
3. Новая методика скоринга (две методики — для ПВУ и для приточных).
4. Корректный перенос полных данных из двух Excel-файлов.
5. Улучшения UX/UI.

---

## 2. Источники данных

| Файл | Тип | Строк данных | Лист |
|------|-----|--------------|------|
| `Конкурентный_анализ_приточно-вытяжные установки.xlsx` | `pvu` | 347 | «Данные» |
| `Конкурентный_анализ_приточные уставновки.xlsx` | `supply` | 237 | «Данные» |

Оба файла: 41 столбец **A–AO**, строка 1 — заголовки, данные со строки 2.
Пустые значения = `н/д` (трактуются как отсутствие данных).

### 2.1 Карта столбцов (A–AO → поля модели)

| Кол. | № | Заголовок | Поле в data.json |
|------|---|-----------|------------------|
| A | 1 | Бренд | `brand` |
| B | 2 | Цена, руб | `price` (+ `price_num`) |
| C | 3 | Наименование | `name` |
| D | 4 | Ссылка с сайта | `url` |
| E | 5 | Расход воздуха мин/мах, м³/ч | `flow` (+ `flow_max`) |
| F | 6 | Свободный напор мин/мах, Па | `pressure` |
| G | 7 | Размеры (Ш×В×Г), мм | `dims` (+ `thickness`) |
| H | 8 | Размеры с учётом патрубков, мм | `dims_ports` |
| I | 9 | Питание, ф/В/Гц | `power` |
| J | 10 | Степень защиты IP | `ip` |
| K | 11 | Класс электрозащиты | `shock` |
| L | 12 | Уровень шума, дБ(А) | `noise` (+ `noise_max`) |
| M | 13 | Наличие воздушного клапана | `valve` |
| N | 14 | Привод клапана | `valve_drive` |
| O | 15 | Тип электропривода | `drive_type` |
| P | 16 | Наличие воздушного фильтра | `filter` |
| Q | 17 | Класс фильтрации | `filter_class` (+ `filter_rank`) |
| R | 18 | Наличие рекуперации | `recup` |
| S | 19 | Тип рекуператора | `recup_type` |
| T | 20 | Производитель рекуператора | `recup_maker` |
| U | 21 | КПД рекуператора | `recup_eff` (+ `eff` — только ПВУ) |
| V | 22 | Наличие нагревателя | `heater` |
| W | 23 | Тип нагревателя | `heater_type` |
| X | 24 | Тип нагревательного элемента | `heater_elem` |
| Y | 25 | Наличие охладителя | `cooler` |
| Z | 26 | Тип охладителя | `cooler_type` |
| AA | 27 | Тип вентилятора | `fan_type` |
| AB | 28 | Тип двигателя | `motor` |
| AC | 29 | Модель | `model_code` |
| AD | 30 | Питание (вентилятора), ф/В/Гц | `power_fan` |
| AE | 31 | Установка 2-х вентиляторов | `two_fans` |
| AF | 32 | Автоматика | `auto` |
| AG | 33 | Производитель контроллера | `controller` |
| AH | 34 | Наличие пульта | `remote` |
| AI | 35 | Тип пульта | `remote_type` |
| AJ | 36 | Wi-Fi | `wifi` |
| AK | 37 | (заголовок «Функциональные возможности:») | — (пропуск) |
| AL | 38 | VAV-регулирование | `vav` |
| AM | 39 | Управление влажностью | `humidity` |
| AN | 40 | Датчик CO₂ | `co2` |
| AO | 41 | Дополнительные требования | `extra` |

`type` — добавляется при сборке: `pvu` или `supply`.

---

## 3. Извлечение числовых величин (парсеры)

Грунтуется на реально встречающихся форматах из обоих файлов.

- **Расход** `flow_max` = максимальное целое из E. `«90-580»→580`, `«650»→650`.
- **Цена** `price_num` = целое из B, убрать пробелы/валюту. `«449500 ₽»→449500`.
- **Толщина** `thickness` = из G взять все целые ≥ 50, вернуть **минимальное**.
  `«968×760×568»→568`, `«660×706×280»→280`, `«н/д»→null`. Разделители `× х x *`.
- **Шум** `noise_max` = максимальное целое из L в диапазоне 10…90. `«30-47»→47`,
  `«57-70»→70`, `«н/д»→null`.
- **КПД** `eff` (только ПВУ) = первое число из шаблона `NN%` в U. `«до 89%»→89`,
  `«78%»→78`. У приточных U = `нет`/`н/д` → не используется.
- **Класс фильтра** `filter_rank` = по строке Q максимальный ранг по лестнице (см. 3.1).
- **Функции** `func_count` = число ячеек со значением **ровно «да»** среди
  {Wi-Fi (AJ), VAV (AL), Влажность (AM), CO₂ (AN)}. Диапазон 0…4.
  ⚠️ `опция` и `н/д` **не считаются** (строгое соответствие «да»).

### 3.1 Лестница рангов класса фильтрации

```
H14=18 · H13=17 · H11=15 · HEPA=15 · E12=14 · E11=13 · EPA=12 · E10=12 ·
EU9=11 · F9=11 · F8=10 · EU7=9 · F7=9 · F6=8 · EU5=7 · F5=7 · M6=6 · M5=6 ·
EU4=4 · G4=4 · EU3=3 · G3=3 · G2=2 · ПЫЛЕВОЙ=2 · G1=1
```

Алгоритм: Q привести к ВЕРХНЕМУ регистру, убрать пробелы; для каждого ключа лестницы
проверить вхождение подстрокой; вернуть **максимальный** найденный ранг; нет совпадения
→ нет данных (`null`).

Проверка на реальных значениях:
`«G4/HEPA (H11)»→15`, `«EPA E11»→13`, `«G4/F5/F7/F9»→7(F5)…max F9=11`→`11`,
`«пылевой»→2`, `«Префильтр высокой плотности»→null (нет данных)`, `«G4»→4`, `«EU5»→7`.

---

## 4. Сегментация по расходу (макс. расход)

Границы: `lo < расход ≤ hi`.

**ПВУ (8 сегментов):**
| Сегм. | Диапазон, м³/ч |
|------|----------------|
| S1 | ≤ 250 |
| S2 | 251–400 |
| S3 | 401–550 |
| S4 | 551–900 |
| S5 | 901–1300 |
| S6 | 1301–1700 |
| S7 | 1701–2500 |
| S8 | > 2500 |

**Приточные (7 сегментов):**
| Сегм. | Диапазон, м³/ч |
|------|----------------|
| S1 | ≤ 500 |
| S2 | 501–800 |
| S3 | 801–1400 |
| S4 | 1401–2500 |
| S5 | 2501–3500 |
| S6 | 3501–5000 |
| S7 | > 5000 |

---

## 5. Баллы по параметрам (каждый 0–100)

Баллы считаются **внутри своего типа** (приточные — отдельно от ПВУ): сегментные
границы и глобальные min/max берутся по выборке этого типа.

### А) Сегментные (нормировка ВНУТРИ сегмента, МЕНЬШЕ = ЛУЧШЕ): Цена, Толщина, Шум
```
балл = 100 × (Макс_сегмента − значение) / (Макс_сег − Мин_сег)
```
min/max считаются по моделям сегмента, у которых параметр есть (не «н/д»).

### Б) Абсолютные (нормировка ГЛОБАЛЬНО по типу, БОЛЬШЕ = ЛУЧШЕ): Фильтр, Функции, КПД
```
Фильтр и КПД: балл = 100 × (значение − Мин_глоб) / (Макс_глоб − Мин_глоб)
Функции:      балл = значение / 4 × 100        (0→0, 1→25, 2→50, 3→75, 4→100)
```

### Правила краёв (для всех параметров)
- **Нет данных (н/д) → балл = 0** (штраф за неполноту, НЕ пропускать).
- **Макс = Мин (нет разброса) → балл = 50** (нейтрально, НЕ 100).
- Итог зажимается в [0, 100].

---

## 6. Итоговый балл

Среднее арифметическое **всех** параметров (равные веса), пропуски входят как 0.

- **ПВУ (6 параметров):** `ИТОГ = (Цена + Толщина + Шум + Фильтр + Функции + КПД) / 6`
- **Приточные (5 параметров):** `ИТОГ = (Цена + Толщина + Шум + Фильтр + Функции) / 5`

Чем больше пропусков — тем ниже ИТОГ (неполные позиции не завышаются).

**Эталон сегмента** = позиция с максимальным ИТОГ внутри сегмента (бейдж в карточке).
Ранжирование: по убыванию ИТОГ (внутри сегмента — честное сравнение однотипных по
размеру; глобально по типу — общий рейтинг).

### Оговорки
- Шум измеряется производителями по-разному (L wA у SHUFT/Breezart; звуковое давление
  у Turkov). Балл шума приблизительный, вес 1/6 (ПВУ) или 1/5 (приточные).
- КПД обязателен для ПВУ (рекуперативные установки) и отсутствует у приточных.
- Веса всех параметров равные.

---

## 7. Архитектура приложения и UX

### 7.1 Переключатель типа (главный элемент навигации)
Сегмент-контрол **«Приточные | Приточно-вытяжные»** вверху. Активный тип управляет:
каталогом, фильтрами, таблицей сравнения, справочником и калькулятором. Состояние —
в URL (`?type=supply|pvu`). Выбор моделей хранится **отдельно для каждого типа**;
сравнение — только в пределах одного типа (баллы типов несопоставимы — разное число
параметров и разные сегменты).

### 7.2 Фильтр по сегменту (новое)
Так как методика сегментная, добавляется фильтр «Сегмент (класс по расходу)» —
позволяет сравнивать модели одного размерного класса. Бейдж сегмента — в карточке и
в сравнении.

### 7.3 Карточка модели
Бренд (Rusklimat → **SHUFT**, подсветка «наш бренд»), наименование, балл 0–100 со
звёздами, бейдж сегмента (+«эталон» для лидера сегмента), ключевые характеристики
(расход, шум, фильтр), цена.

### 7.4 Сравнение
В пределах активного типа. Добавляется:
- строка «Оценка (0–100)»;
- раскрываемый **«Разбор оценки»** — под-баллы по 6/5 параметрам (полоски);
- строка «Сегмент»;
- для приточных секция «Рекуперация»/КПД скрывается (нерелевантна).

### 7.5 Справочник и калькулятор
Переписываются под новую методику и зависят от активного типа (6 vs 5 параметров,
сегментные vs абсолютные, правила краёв, лестница фильтра, таблицы сегментов).
Старый переключатель «статический/динамический» удаляется (методика всегда
относительна выборке типа).

---

## 8. План изменений по файлам

| Файл | Изменения |
|------|-----------|
| `tools/build_data.py` | **новый**: сборка `data.json` из двух xlsx (парсеры, type, parsed-поля) |
| `data.json` | регенерируется: 347 ПВУ + 237 приточных, с `type` и parsed-полями |
| `rating.js` | новый движок: сегментация + сегментные/абсолютные баллы + правила краёв + среднее; per-type bounds; справочник + калькулятор; удалить static/dynamic |
| `app.js` | состояние типа, выбор per-type, фильтр сегмента, рендер по типу, разбор оценки, условные секции |
| `index.html` | логотип SHUFT, переключатель типа, фильтр сегмента, тексты tools |
| `styles.css` | логотип, сегмент-контрол, бейджи сегмента/эталона, полоски разбора, подсветка SHUFT, адаптив |
| `README.md` | обновление под два типа и новую методику |

---

## 9. Открытые решения (нужно подтверждение)

1. **Только сравнение внутри типа** (приточные ↔ приточные, ПВУ ↔ ПВУ), без смешивания.
2. **`опция` у Wi-Fi не считается функцией** (строго «да»). Затрагивает 96 строк ПВУ.
3. **Rusklimat показывать как SHUFT** и подсвечивать как «наш бренд».
4. Текстовые фильтры без класса (`Префильтр высокой плотности` и т.п.) → балл фильтра 0.

---

## 10. Подбор аналога (SHUFT ⇄ конкуренты)

### 10.1 Задача
Пользователь видит модель конкурента (например, Turkov) и хочет эквивалент от
SHUFT — или наоборот: по модели SHUFT увидеть, какие модели конкурентов она
закрывает. Результат должен быть объясним (видно, по каким параметрам совпадение)
и сразу сравним (кнопка «Сравнить» ведёт в существующую таблицу сравнения).

### 10.2 Кандидаты (жёсткие условия, v2)
1. **Тот же тип установки** (`supply`/`pvu`) — между типами аналоги не ищутся.
2. **Направление по бренду:** исходная НЕ SHUFT → кандидаты только SHUFT;
   исходная SHUFT → кандидаты все остальные бренды.
3. **Расход обязателен** у обеих сторон; окно **асимметричное −25% … +40%**
   (кандидат слабее оригинала опаснее, чем запас сверху). Если кандидатов в узком
   окне < 3 — окно автоматически расширяется вниз до −40% (в модалке — плашка
   «окно расширено»).
4. **Питание:** конфликт 220В ⇄ 380В — блокер (замена требует перекладки сети).
   «220/380» (универсальное) совместимо с обоими; «н/д» не блокирует (⚠-пометка).
5. **Рекуператор (только ПВУ):** роторный ⇄ пластинчатый/энтальпийный — блокер
   (разная физика: влагоперенос, обмерзание). Пластинчатый ⇄ энтальпийный —
   допустимо (штраф в балле, п. 10.3). «н/д» не блокирует (⚠-пометка).
6. Нагреватель — блокер при несовместимых типах (см. 10.6).
Переключатель «показать несовместимые» в модалке раскрывает кандидатов,
отсеянных п. 4–6, с красными пометками причин.

### 10.3 Балл соответствия (0–100, v2)
Взвешенная сумма мер близости. Цена в балл **не входит** — она тай-брейкер
ранжирования (п. 10.4) и бейдж Δ цены. Отсутствующие данные (у любой из сторон)
исключают параметр, веса перенормируются; в результате показывается
«совпадение по N из M параметров». Лишние функции аналога не штрафуются.

| Параметр | Вес ПУ | Вес ПВУ | Мера близости (0–100) |
|---|---|---|---|
| Расход | 35 | 30 | асимметрично: выше → 100×(1−ΔF/0.40); ниже → 100×(1−|ΔF|/0.25) |
| Нагреватель | 15 | 10 | тип совпал → 100 («вода+эл.» покрывает оба; «нет»+«нет» → 100), иначе 0 |
| Функции | 15 | 10 | покрытие набора оригинала: k из m функций → k/m × 100; m=0 → 100 |
| Шум | 10 | 8 | |Δ| ≤ 2 дБ → 100, линейно до 0 при 15 дБ |
| Толщина | 10 | 8 | 100 × (1 − (|Δt|/tо)/0.50): 0% → 100, 50% → 0 |
| Фильтрация | 10 | 9 | ранг аналога ≥ оригинала → 100; ниже → 100 − 20 × Δрангов (мин 0) |
| Двигатель (EC/AC) | 5 | 5 | совпал или кандидат EC при оригинале AC → 100; AC вместо EC → 0 |
| КПД (только ПВУ) | — | 12 | 100 × (1 − (|Δη|/ηо)/0.30): 0% → 100, 30% → 0 |
| Тип рекуператора (ПВУ) | — | 8 | семейство идентично → 100; пластина⇄энтальпия → 70 |

Суммы весов: ПУ = 35+15+15+10+10+10+5 = 100; ПВУ = 30+10+10+8+8+9+5+12+8 = 100.

### 10.4 Ранжирование и вывод (v2)
- Сортировка по убыванию балла; **при разнице баллов ≤ 5 выше встаёт кандидат
  выгоднее по цене** (кандидаты без цены — вниз пары); далее — рейтинг модели.
- Δ цены — бейдж «дешевле/дороже на N ₽ (±X%)» у каждого кандидата.
- Показываются до **5** кандидатов с баллом ≥ **50**.
- Категории: ≥85 «точный аналог» · 70–84 «близкий» · 50–69 «частичный».
- Пусто → «Аналогов не найдено» с причиной (нет расхода / нет моделей в окне /
  все отсеяны жёсткими фильтрами — с подсказкой включить «показать несовместимые»).
- Валидация v2 на базе 1013 моделей (без снятых линеек SKOYOD/Allston):
  0 нарушений жёстких правил, 0 нарушений тай-брейкера, покрытие 628/781
  моделей с кандидатами в окне, средний балл топ-1 = 76.

### 10.5 UI/UX (не трогает существующую логику)
- Кнопка на карточке: у конкурентов «⇄ Аналог от SHUFT», у SHUFT «⇄ Аналоги конкурентов».
- **Модальное окно**: сверху оригинал (компактно: расход, нагреватель, шум, цена),
  ниже список кандидатов — % соответствия, «по N из M параметров», ключевые
  параметры, Δ цены (зелёным, если аналог дешевле); у каждого кнопка «Сравнить»
  (выбор = [оригинал, аналог] → скролл к таблице), сверху «Сравнить топ-3»
  (оригинал + 3 лучших = 4 слота — максимум сравнения).
- Закрытие: ×, клик по фону, Esc. Мобайл — полноэкранный лист.
- Подбор — надстройка: отдельный модуль `analog.js`, движок рейтинга и сравнение
  не меняются; карточка получает только дополнительную кнопку.

### 10.6 Принятые решения
Подтверждены 2026-07-02 (v1):
1. **Нагреватель — гибрид:** по умолчанию показываются только совместимые по типу
   нагревателя; «н/д» не исключает кандидата — параметр выпадает из балла.
2. **Охват — только SHUFT ⇄ конкуренты** (конкурент → SHUFT; SHUFT → конкуренты).
3. **«Сравнить» заменяет** текущий выбор на [оригинал, аналог].

Подтверждены 2026-07-23 (v2, после актуализации базы до 1017 моделей):
4. **Жёсткие фильтры:** питание 220⇄380В и семейство рекуператора (ПВУ) — блокеры
   с фолбэком для «н/д» (⚠-пометка); единый переключатель «показать несовместимые».
5. **Цена — тай-брейкер:** при |Δ балла| ≤ 5 выше кандидат выгоднее по цене;
   в сам балл цена не входит.
6. **Окно расхода асимметричное −25%/+40%** с авторасширением до −40%, если
   кандидатов < 3.

### 10.7 Файлы и этапы
| Файл | Роль |
|---|---|
| `analog.js` | движок подбора: конфиг весов, меры близости, findAnalogs() |
| `app.js` | кнопка на карточке, модалка, интеграция со сравнением |
| `index.html` | контейнер модалки, подключение analog.js |
| `styles.css` | стили модалки и бейджей соответствия |

Этапы: 1) логика (этот раздел) и подтверждение решений → 2) `analog.js` +
валидация на реальных данных (node-прогон: примеры Turkov→SHUFT, покрытие,
распределение баллов) → 3) UI (кнопки, модалка, сравнение) → 4) проверка в
превью (оба типа, оба направления, крайние случаи) → 5) README, коммит, деплой.

---

## 11. Аргументы продажи (автосводка в сравнении)

### 11.1 Задача
Менеджер сравнивает модель SHUFT с конкурентом — сервис сам формулирует готовые
аргументы («дешевле на 464 169 ₽ (−30%)», «тише на 4 дБ», «есть Wi-Fi и CO₂,
которых нет у конкурента») и даёт кнопку «Скопировать» для вставки в письмо.

### 11.2 Когда показывается
- В выборе сравнения есть **≥1 модель SHUFT и ≥1 конкурент** (иначе блока нет).
- Референс SHUFT = первая выбранная модель SHUFT.
- На **каждого конкурента** в выборе — свой подблок аргументов (до 3).
- Блок располагается между заголовком «Сравнение моделей» и таблицей.

### 11.3 Аргументы и пороги (утверждаем только при наличии данных с обеих сторон)
Первый пункт — всегда **контекст производительности** (честность сопоставления):

| Параметр | Условие | Формулировка |
|---|---|---|
| Расход | \|Δ\|/конк ≤ 15% | «сопоставимая производительность (X против Y м³/ч)» |
| Расход | SHUFT выше > 15% | «производительность выше на N м³/ч» |
| Расход | SHUFT ниже > 15% | слабое место: «производительность ниже (X против Y)» |
| Цена | дешевле ≥ 10 000 ₽ **или** ≥ 3% | «дешевле на X ₽ (−P%)» |
| Шум | тише ≥ 3 дБ | «тише на N дБ» |
| Толщина | тоньше ≥ 30 мм **или** ≥ 10% | «компактнее по толщине на N мм» |
| Фильтрация | ранг выше | «класс фильтрации выше (A против B)» |
| Фильтрация | ранг равный | «фильтрация не хуже (A)» |
| Функции | есть у SHUFT, нет у конкурента | «есть Wi-Fi, VAV, датчик CO₂ — у конкурента нет» |
| КПД (ПВУ) | выше ≥ 3 п.п. | «КПД рекуперации выше на N п.п.» |

Пороги отсекают «аргументы» в пределах погрешности (тише на 1 дБ — не аргумент;
методики измерения шума у брендов различаются — в блоке даётся сноска).
Нет данных у любой из сторон → параметр не упоминается вовсе.

### 11.4 Слабые места (решение 11.6)
Зеркальные условия (дороже / шумнее / толще / класс ниже / у конкурента есть
функции, которых нет у SHUFT / КПД ниже) — мелкой серой строкой «обратите
внимание», чтобы менеджер был готов к возражениям. Если преимуществ нет вовсе —
честное «Прямых преимуществ по опубликованным данным не найдено» + слабые места.

### 11.5 Копирование
Кнопка «Скопировать» у каждого подблока → плоский текст:
`SHUFT {модель} против {бренд} {модель}:` + строки-аргументы через «—».
Копируются только преимущества (слабые места — служебная подсказка менеджеру).

### 11.6 Принятое решение (2026-07-02)
Преимущества — крупными зелёными чипами; «слабые места» — мелкой серой строкой
«обратите внимание» (менеджер готов к возражениям). В копирование слабые места
не попадают.

### 11.7 Реализация
Функция `renderSaleArgs(models)` в `app.js` (вызов из `renderCompare`), стили
чипов в `styles.css`. Новых файлов и изменений движков не требуется.
